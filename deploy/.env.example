# Taler Docker Configuration
# Copy this file to .env and customize if needed

# Data directory inside container (optional, defaults to /data)
# TALER_DATA=/data

# Config file path inside container (optional, defaults to /taler.conf)
# TALER_CONF=/taler.conf
